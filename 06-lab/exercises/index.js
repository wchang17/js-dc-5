const one = require('./one.js') //the require command pulls in a function or json data
// one()
const two = require('./two.js')
console.log(two)

// two.twoAsNum() //calling functions inside of objects